To start the To-Do List application, enter the todo_list directory and run the following commands:

```
export NODE_OPTIONS=--openssl-legacy-provider
npm install --save -s react react-dom react-scripts web-vitals
npm start
```
